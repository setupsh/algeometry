using UnityEngine;

namespace UI {
    public class IndicatorAdder : MonoBehaviour {
        private IndicatorsList _indicatorsList;

        private void SetIndicatorsList(IndicatorsList indicatorsList) {
            _indicatorsList = indicatorsList;
        }

        public void ShowMenu() {
            
        }
    }
    
}
